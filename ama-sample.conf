{
  #required
  pg => 'postgresql://mojo:mojo@/test1',
  #recommended
  secrets => ['mojo'],
  #leave blank to remove login functionality
  oauth2 => {
    #leave blank to remove facebook login
    facebook => {
            key => 'referred to as app ID',
            secret => 'referred to as app-secret',
    },
    #leave blank to remove google login
    google => {
            key => 'referred to as client ID',
            secret => 'referred to as client secret'
    },
  },
  #required -2 recommended
  vote_floor => -int,
  #leave blank to remove email feedback functionality
  sendgrid => {
    apikey => 'key',
    to     => q(email),
    #cc     => q(email),
    #bcc    => q(email),
    from   => q(email),
  },
};
